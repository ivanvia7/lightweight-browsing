# Decisions — lightweight-browsing

Append-only history. Newest last. Decisions (what was chosen, why, what it rules out) and
shipped work (what landed, why) so a future session reads this instead of re-deriving it.

---
